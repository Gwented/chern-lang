@def
    var->
                    // Why was it called family 💀

                    // Maybe this should just stay simple...
         valid: S|CCODE [Range(0..=5), Range(0,)]

        enum: E|Enum

    nest->
        .enum {}
@end
@def
    bind -> "@./real_path"
var->
    expr: S|Expr [!IsEmpty] #wall #wextra #walla #wab #last #last #Last
    // Argument parser should be more like this
    let: Map<i3, "hi">
