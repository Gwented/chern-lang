The process of type checking a serialized file before deserializing should be simple.
It sees "name", stores that as the id, sees "32" and stores it, compares the identifier to it's interned,
checks if the number is in the range of it's data type, checks any conditions, then continues.

@NOWNOW A system where a hashmap is stored that links to a symbol.
Make ExprHir do specifics like, "Add(Node, Noded)" over a generic binary op.
Example:
    let y = x + 2
    let x = 3

when y = x + 2 is seen x is stored in a hashmap and y is stored (somewhere) with dependencies so 
is there are more such as y = x + z the z can be stored and we can just iterate through deps of y
for backoff logic. how y is stored is fortnite.

@NOW Alias that has more functional patterns such as ret type @POST Function constraint resolution

PRIORITY:- TURN INTO MONO REPO

@
- Functions within conditions need to be seen as expressions in HIR

- Still no unicode lexing

- If a path is invalid to have it's name generated in a program, then it should be checked for
  and be a hard error if the user doesn't make an "as" alias
-=_+_+-=
Add control flow for import pass to report detailed errors
Add peek behind char behind before reading for a keyword

__+_+_

- Need to explicitly recognize comments within lexer for querying purposes

- Should maybe return unfinished AST

- Parser should make diagnostics for identifier having values in a more explicit manner, as opposed to 
  allowing for something like "found illegal 'illegal $'"

- Functions are still not able to be evaluated semantically.

- Section arenas

- Should recommend help with "e#" escaping if the user uses a keyword when escape is needed.

- Add help to lsp diagnostic

- Min and max for stuff like "i32.MAX"

- Need keyword specific string printing to be doable

- Tuples need a builtin type conversion since it is, builtin,

- bind should probably be a statement or item

- let and alias not done

- There is no way to member access values, for the case self.something. Dot overall isn't done.

- Way for the user to adjust bind to take priority if in a serialized file or not

- Need comprehensive system that allows for module errors to suggest similar things found
  May use enums for this like, Similar::Keyword(err_name), Similar::ModuleName(module, err_name), etc

- Dot referencing related errors are bad unless keywords are added

- What if, if there were "Happy path" modules when diagnostics do exist, we waited until
  the happy path was done, THEN reported?

- Cross module error messages

- Lsp needs better spanning information from diagnostic structures

- Lsp should re-use interner for entire duration. Some extra caching, etc.

PRIO - Imports needs disallow '\'

- Module resolution should send detailed errors upon recognized io errors

- If a module has name, "defs" and there is another imported named "defs", there should be an explicit
  check for an alias then if found, one now ONLY uses it's alias so it allows for same name imports

- Probability model isn't in true state

- Possibly dangerous peek behind usage in parser

- Contexts should have wrappers for reporting so their internal data can be automatically filled
  to avoid mass renames upon internal changes such as can_color

@defvar->name:str,age:u8@end

__ CLI __
- fmt
- add fmt -c/--compact
@

SERIAL
@
- Should be parallelizable 
@


PLAN:
The plan later on is to create a single table that doesn't care about modules or anything that is a 
HIR which the serial side uses for it's cases. Final IR will be made after. 

Highly favoring towards the first stage of serial doing in-line lexing if needed rather than fully lexing
the serialized file.

------ ISSUES ---- 
This will only print the beginning of the comment.
```

            /* /* */
            
```
BG: The line view does not explicitly declare a new line that does not have an ending new line, 
as a new line. Here, eof is gone (I think?) as it just ends with WS, which isn't eof. This can be fixed
by just pushing an eof line only if the span.end == eof_pos but that then causes other compatibility
issues because we now have a line that is eof, which isn't actually a new line that needs to be treated
as another line because it IS a new line.
