//IGNOREME:
//
//Ideal scenario is, given a set of say, 10 tokens, the model will query as different known langauge
//concepts, like a type setting, and give a probability. So, it would be a function given inputs
//like current section, a window of tokens and maybe some general semantic info of what was being
//done before the occurrence (Although that might be a bit much). Then it will output a structure
//that contains probability of what the tokens it viewed are more structurally similar to.
