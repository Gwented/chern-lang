[package]
name = "website"
version.workspace = true
edition.workspace = true

[dependencies]
chrn_utils = { path = "../chrn_utils" }
# Ok now this just looks bad
macrosc = { path = "../../common/macrosc/" }
