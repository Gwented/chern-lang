@def
import "../../defs.chrn"

// alias default(x, y) = [Contains(x = "Defaulting", y = "Forklift")] #warn

let input = "Chrn"

var->
    x: i32

                // Gilber let b = 5

        //let r = 3.5// Testing Stuff
nest->
    enum State {
        input:  core.BigInt
        Ready: main.Book
        Stopped: nil
    } #scient

    struct Book {
        len: str
    } #ignore

@end

export let /*letting*/ number/*variable*/= /*expr*/ 0x23
var->
                        x: i32    // Hi
nest->
struct Person {
    name:str
    age:    u8}


