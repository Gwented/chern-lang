
@def
import "../../defs.chrn"

// alias default(x, y) = [Contains(x = "Defaulting", y = "Forklift"), if(2 + self == 3)] #warn

let input = "Chrn"

let stuff = 3

// Ok please stop
let ffi = IsEmpty

let a = b
let b = c
let c = 3
let x = (2 + 2) == 4
let f32 = 3
let d = other.NUMBER

// alias default() = []

var->
    //x: other.Person [IsEmpty] #ignore #warn
nest->
    struct Person {
        p: Person
    } [IsEmpty] #warn
@end
                // Gilber let b = 5

        //let r = 3.5// Testing Stuff
nest->
    struct State {
        input:  str [] #warn
    }

    enum Book {
        len: sized []
    }
@end

export let /*letting*/ number/*variable*/= /*expr*/ 0x23
var->
                        x: i32    // Hi
nest->
struct Person {
    name:str
    age:    u8}


