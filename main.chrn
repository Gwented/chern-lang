
/* Hello I am a comment */   

@def
import "../../defs.chrn"
//let x = 3// I am another

alias default(x, y) = [Contains(x = "Defaulting", y = "Forklift")] #warn
let b = default(_, _)

let input = "Chrn"

let x = IsEmpty

                // Gilber let b = 5

        //let r = 3.5// Testing Stuff
nest->
    enum State {
        Ready: State
        Stopped: nil
    } #scient

    struct Book {
        len: str
    } #ignore

@end

export let /*letting*/ number/*variable*/= /*expr*/ 0x23
var->
                        x: i32    // Hi
nest->
struct Person {
    name:str
    age:    u8}


