@def
// import "../../defs.chrn"

// alias default(x, y) = [Contains(x = "Defaulting", y = "Forklift"), if(2 + self == 3)] #warn

let input = "Chrn"

let ffi = IsEmpty

let a = b
let b = c
let c = 3

var->
    x: i32 [IsEmpty] #ignore

@end
                // Gilber let b = 5

        //let r = 3.5// Testing Stuff
nest->
    struct State {
        input:  str [] #warn
    }

    enum Book {
        len: sized []
    }
@end

export let /*letting*/ number/*variable*/= /*expr*/ 0x23
var->
                        x: i32    // Hi
nest->
struct Person {
    name:str
    age:    u8}


