use chrn_utils::intern::Intern;
use compilation::script_compiler::ScriptCompiler;

use crate::dump_settings::DumpSettings;

pub fn dump_env(compiler: &ScriptCompiler, interner: &Intern, settings: &DumpSettings) -> String {
    todo!()
}
